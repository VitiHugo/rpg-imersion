import { Container, Typography } from "@mui/material";

interface Props {
  areas: any
}

export function SelectAreaNpc({ areas }: Props) {
  return (
    <Container>
      <Typography>
        NPC`s
      </Typography>
      <Typography>
      
      </Typography>
    </Container>
  )
}